import React from "react";
import ReactDOM from "react-dom/client";
import OCRMediaRevisionTrainer from "./App";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <OCRMediaRevisionTrainer />
  </React.StrictMode>
);
